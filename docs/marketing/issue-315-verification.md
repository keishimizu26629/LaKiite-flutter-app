# #315 / #310 読み仮名・用途改善の検証記録

実施日: 2026-09-08

## 対象と境界

- ブランチ: `codex/issue-315-store-clarity`。ベース: `18d3c3a767973c592d6fad4d5d4b9bdd33a50618`（dev）。
- LPの文言・メタデータ・ブランド行のCSS、ストア文言2案、7枚の構成案、ドキュメントと静的テスト。
- Flutter/Dart、依存関係、Firebase設定・Rules、認証・招待処理・Airbridgeの変更なし。
- Store Console入力、画像アップロード、審査提出、本番公開、マージは実施していない。

## 自動検証

| 検証 | 結果 | 検証できる範囲 |
|---|---|---|
| `node --test scripts/test_web_landing.mjs scripts/test_store_marketing.mjs` | 23件成功、0失敗（LP18、ストア5） | 既存リンク・メタデータの契約、かなと用途、文字数、7枚の順序・再撮影フラグ、安全なDOM生成 |
| `node --check docs/marketing/store/ja-JP/preview.mjs` | 成功 | プレビューJSの構文 |
| `node --check scripts/test_store_marketing.mjs` | 成功 | 新テストの構文 |
| `fvm flutter analyze --no-pub` | No issues found | Flutter静的解析。アプリを実行した証明ではない |
| `git diff --check` | 成功 | 差分の空白エラー |

TDD: 変更前のLP17件成功を確認後、仕様テスト追加時に23件中8件失敗（旧title、別名・h1未変更、ドラフト未作成）を確認。実装後23件成功。さらに、リスト表現と見出しの行分割契約を先に追加してストア5件中3件の失敗を確認し、文案と表示を直して成功させた。

ネイティブのビルド・全Flutterテスト・実機/Simulator操作はこの変更では実施していない。コンパイル対象のアプリコードは変更しておらず、ここでアプリ全体の動作保証を追加したとは言わない。

## ブラウザ確認

既存ユーザーブラウザのプロファイルを変更・終了せず、ローカルに導入済みのPlaywrightから独立したheadless Chromiumを起動。公開LPファイルと構成案だけを許可した127.0.0.1のHTTPサーバーを使用し、秘密設定や`.ai`は配信していない。

20条件で成功:

- LP / 構成案それぞれ、幅320・390・769・1440px × ルート文字16/32px: 16条件。
- LP / 構成案それぞれJavaScript無効: 2条件。
- 構成案JSONの取得失敗 / 参考画像の取得失敗: 2条件。

確認内容:

- LP: title、h1、読み仮名、4つのストア直リンク（冒頭/末尾）、法務リンク、用途説明・文字の矩形。横スクロールだけでなく要素内部の文字あふれも検査。
- 各画面幅・文字サイズでTab移動が両ストアリンクへ到達し、フォーカス枠が表示される。使い方リンクは`#features`へ移動する。
- reduced motionで滑らかなスクロールと装飾アニメーションが停止。
- 構成案: 7枚の入稿不可表示がカード内に収まる。参考画像5枚は読め、未撮影2枚はプレースホルダー。READMEへのリンクはHTTP 200。
- JS無効でもLPのブランドとリンクが残り、構成案ではJSON参照への案内が読める。
- JSON失敗時は空のカード領域とエラー案内、画像失敗時は代替表示。例外を画面外に放置せず、入稿不可表示も残る。
- 通常表示でページ内JSエラーとローカルHTTP 4xx/5xxは0件。

初回確認で、構成案を固定9:16比率にすると320px・文字200%で「入稿不可」が切れることを再現。原因は固定の高さに見出しと注意書きが収まらないこと。プレビューは内容に合わせて伸びる高さに変更し、同じ条件で再検証した。通常サイズの先頭3枚は意味の区切りで改行する。

スクリーンショットを目視: LPの390/1440px、構成案の先頭3枚、320px・文字200%の注意書き。ローカル検証スクリプトと画像は一時保存先 `/tmp/lakiite-store-clarity.8uNI0r/` にあり、配布成果物や入稿素材には含めない。これはChromiumでの確認であり、Safari/iOS、Androidネイティブの検証ではない。

## コードレビュー

code-reviewerの品質・セキュリティ・保守性の観点で実差分をセルフレビュー。

- Critical: 未解消の指摘なし。
- 修正済み: 小画面・拡大文字で構成案の注意書きが欠ける問題。表示高さとブラウザの矩形確認で対応。
- 修正済み: 「共有する相手を選ぶ」だけでは個別ユーザー選択と解釈される余地。説明を現行UIのリスト選択に合わせた。
- 確認: DOM生成は`textContent`中心で、JSONをHTMLとして挿入しない。読み込み失敗時のフォールバックあり。外部依存追加なし。
- 確認: 既存ストアURL、canonical、法務ページ、イベント送信の有無は維持。構成案はHosting公開先`web/`の外。
- 未確認: 第三者による独立レビューは未実施。構成案の写真・掲載対象版との整合は再撮影時に確認が必要。

## 残る承認・公開前作業

1. オーナーが推奨名・説明と7枚の構成を確認する。
2. 対象リリースの両OSでテストデータを使って再撮影する。特に旧タイムライン画像は自分の予定であり、友だちの予定の証拠にはしない。
3. 更新説明を対象版の実差分と照合する。
4. レビュー・マージ後のdev Hostingと公開前チェックを別途実施する。
5. ストア掲載と本番LP公開の明示承認後に反映し、前後14日の同条件集計を確認する。

#315と#310は`Refs`で関連付け、今回のPR作成だけで自動クローズしない。CIは既存`ci.yml`が手動実行のみで、NodeのLP/ストアテストは実行しない。ローカル23件の結果と、別途実行したCIの結果を混同しない。
