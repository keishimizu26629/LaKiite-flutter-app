# #315 / #310 読み仮名・用途訴求の改善計画

更新日: 2026-09-08

進捗: タスク1〜6のローカル実装・調査・検証まで完了。タスク7のdev向けPRでレビューを依頼する。実施結果と公開前の残作業は [検証記録](issue-315-verification.md) を参照。

対象読者: LP・ストア素材の制作担当と公開判断を行うオーナー。

## 目的と承認範囲

「ラキーテ」と聞いた人が製品名を識別でき、最初の画面で友だち向けの予定共有カレンダーと理解できる状態にする。
2026-09-08のチャットで、前回の改善案とカタカナ表記を含めた計画・修正を承認済み。
名称案は `LaKiite（ラキーテ）予定共有カレンダー` を推奨する。
既存の「お誘い未満」は捨てず、用途説明の後に価値として伝える。

成果物はLPのコード、ストア文言案、7枚の画像構成案、検証記録とPR。
ストア掲載反映・審査提出・本番Hosting公開は含めない。Issue #315の対象外事項を維持する。
Flutterの挙動、Firebase設定・Rules、認証、Airbridge、ロゴそのものは変更しない。
別作業中のメインworktreeは変更せず、`origin/dev`の`18d3c3a`を起点にhatcherで作業場所を分離した。

## 実行タスク

### 1. 現状と根拠の確認

- 対象: `web/index.html`、`web/style.css`、`docs/marketing/issue-310-lp-plan.md`、Issue #315、公開ストア。
- 作業: 最新の公式文字数制限、公開競合2件、現在の共有UIと残存するgroup処理を確認する。
- 合格条件: 推測・公開版未確認事項を区別し、採用しない機能表現を明記する。

### 2. 回帰テストを先に追加

- 対象: `scripts/test_web_landing.mjs`、`scripts/test_store_marketing.mjs`。
- 作業: title/OG/Twitterの一致、可視の読み仮名、用途を示すh1、別名の構造化データ、文言制限、7枚の順序、入稿不可表示を検査する。
- 合格条件: 未実装で失敗するログを確認し、実装後に成功する。既存17件も維持する。

### 3. LPを修正

- 対象: `web/index.html`、`web/style.css`、`web/README.md`。
- 作業: titleを `LaKiite（ラキーテ）｜友だちと予定を共有するカレンダー` とし、OG/Twitterを一致させる。
- 作業: 冒頭でブランド名と読み仮名、h1で用途、説明文で「お誘い未満」の価値を伝える。
- 合格条件: ストア直リンク・法務リンク・canonical・計測境界を維持。JS無効でも読み仮名と用途が読める。

### 4. ストア文言を編集可能なファイルにする

- 対象: `docs/marketing/store/ja-JP/copy.json`、`docs/marketing/store/ja-JP/README.md`。
- 作業: 推奨案と比較案、Appleの名前/サブタイトル/キーワード/説明、Playの名前/短い説明/詳細説明、OS別の更新説明を用意する。
- 合格条件: 読み仮名と用途が入る。文字数を機械検査する。曖昧なグループ共有や保証できない効果を訴求しない。

### 5. 7枚の構成案と目視可能なプレビューを作る

- 対象: `docs/marketing/store/ja-JP/screenshots.json`、`preview.html`、`preview.css`、`preview.mjs`。
- 作業: 先頭3枚を「用途→共有→会うきっかけ」に変更し、1枚1メッセージにする。
- 作業: 公開済みLP画像をiOS構成案の参考に使い、入稿不可の表示を各カードへ入れる。
- 合格条件: Android画像をiPhone画像で代用しない。元画像のOS、再撮影条件、公開前チェックを明示する。
- 制約: 既存LPの画像は過去のiOS画面。現在の公開版との一致・テストデータの確認が済むまで、どちらのストアにもアップロードしない。

### 6. 検証とレビュー

- 対象: 変更全体。
- 実行: `node --test scripts/test_web_landing.mjs scripts/test_store_marketing.mjs`、JS構文検査、`fvm flutter analyze --no-pub`、`git diff --check`。
- ブラウザ: LPの320/390/1440px、200%文字、JS無効、キーボード。素材プレビューの読み込み・折り返し・画像・入稿不可表示。
- 合格条件: 実際のコマンド結果を記録。新文言による崩れや既存導線の退行がない。

### 7. PRと公開前チェックポイント

- 対象: `docs/marketing/issue-315-verification.md`、PR本文、Issue進捗。
- 作業: 意図したファイルだけcommit/pushし、dev向けPRを作成する。マージや公開はしない。
- 合格条件: #315 / #310の残作業に応じて`Closes`か`Refs`を選び、未確認事項を隠さない。

## 評価と保守

制作・検証担当: Codex。掲載する文言・素材の最終承認者: プロダクトオーナー。
次の判断: PRとプレビュー確認後、対象リリースに合ったiPhone/Android画面を再撮影する。
公開前後の同じ長さの期間でストア閲覧数・初回DL・閲覧から初回DLへの転換をOS別に見る。
少数データで勝ち案や順位改善を断定しない。まず短時間の閲覧で用途と対象ユーザーが説明できるかを確認する。
ここでの名称・コピー採用は仮説であり、検索順位・DL増・CVR改善の保証ではない。
